# v5+ 未来调度方向

当前 v5 runtime 的边界是：

- 显式 `Runtime` 生命周期
- `Service` / `Job` 分离
- 短任务池化
- 有界 Job 队列
- 队列满策略
- 分区内本地优先级队列
- 分区化 Job 执行

这是当前分支的**有意边界**。

## 为什么当前分支停在这里

当前 runtime 给了三条关键保证：

1. 生命周期与关闭流程的显式所有权
2. 每个分区内部可预测的本地队列语义
3. 分区间隔离

如果直接引入全局公平调度或通用 work stealing，会在没有完整调度器重构的前提下削弱这些保证。

## 何时值得进入更深调度层

只有当基准测试在**真实工作负载**下反复证明“本地分区队列导致容量滞留”时，才应继续演进，例如：

- 相对可比 shared-pool 基线出现明显吞吐损失
- 某分区队列等待显著升高，而其他分区 worker 长时间空闲
- 在调整 worker 数与 queue cap 后，仍持续出现容量滞留

可作为实用阈值：在真实负载中，若仍出现约 **15-25%+** 吞吐差距或 **2x+** 尾部延迟差距，再考虑下一步调度能力。

## 若必须继续，建议的最小下一步

最小且相对安全的方向是：**受限、可选（opt-in）的 borrowing**。

- 空闲 worker 仅可从“显式允许共享”的分区借一个任务
- 默认关闭
- 源分区的 priority/FIFO 顺序仍需保持
- 生命周期、fail-fast、shutdown 语义仍保持全局一致

这比通用 work stealing 风险更低，因为共享范围可控且语义清晰。

## 当前应继续延后的能力

以下能力建议继续不进入当前 v5 主线：

- 所有分区通用 work stealing
- 全局公平调度器
- 基于全局调度器的 soft quota
- 动态分区再平衡
- 无视分区本地 queue policy 的偷取行为

## 后续若探索 borrowing，需要重点关注风险

- 无形削弱 critical-vs-batch 隔离
- 跨分区移动导致 drop-oldest / drop-newest 语义被破坏
- 在当前单 runtime 锁模型上引入新的隐性竞争热点
- 当“分区本地优先级”与“全局公平”叠加时语义变得模糊

## 建议持续观察的 benchmark 指标

- 平衡与倾斜分区负载下的提交吞吐
- 各分区队列等待时延
- 混合短/长任务下的 p95/p99 启动延迟
- 某分区有排队而其他分区空闲的时间占比

若这些指标保持健康，当前“本地分区 + 显式边界”的设计应继续保持。
